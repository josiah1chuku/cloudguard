# CloudGuard — Python dependencies
# Install with: pip install --prefer-binary -r requirements.txt
# Python >= 3.14 required

# Core ML
scikit-learn>=1.9.0
xgboost>=3.2.0
imbalanced-learn>=0.14.2

# Deep learning (Stage 3 CNN-LSTM)
# For GPU (Colab T4): pip install torch --index-url https://download.pytorch.org/whl/cu121
# For CPU only:       pip install torch --index-url https://download.pytorch.org/whl/cpu
# torch>=2.12.0  # Install separately — see above

# Data
pandas>=3.0.3
numpy>=2.4.6

# Visualisation
matplotlib>=3.11.0
seaborn>=0.13.2

# Networking
requests>=2.34.2

# Utilities
tqdm>=4.68.2
python-dotenv>=1.2.2

# Jupyter (for Stage 3 notebook)
jupyter>=1.1.1
ipykernel>=7.3.0
